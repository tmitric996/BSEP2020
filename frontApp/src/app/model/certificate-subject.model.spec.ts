import { CertificateSubject } from './certificate-subject.model';

describe('CertificateSubject', () => {
  it('should create an instance', () => {
    expect(new CertificateSubject()).toBeTruthy();
  });
});
